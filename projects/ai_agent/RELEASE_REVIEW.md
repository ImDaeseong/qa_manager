# ai_agent 무료 소스 공개 검토

상태: **HOLD**. 기준 커밋 `b1d5e60`의 소스 저장소를 전세계에 무료 공개하는 범위다. 실행 서비스나 개별 에이전트의 제품 출시는 이 검토에 포함하지 않는다. 현재 주요 사용자는 저장소 소유자와 개발자이며, 외부 사용자의 역할·지원 약속은 확정되지 않았다. 이 저장소는 독립 에이전트 여러 개와 공통 평가 하네스 설계를 함께 담는다(`README.md`, `SPEC.md`). 각 영역의 아래 근거는 출시 통과 판정이 아니다.

## 기능 적합성

- 기존 근거: 체크리스트의 기능 요구사항과 `README.md`의 프로젝트별 테스트 실행 경로가 있다.
- 남은 검토: 공개 소스 사용자가 재현할 대표 과업을 에이전트별로 정하고, 실제 모델·외부 API를 포함한 성공·실패 결과를 확인한다.

## 성능 효율성

- 기존 근거: `SPEC.md`는 실행 시간·비용 예산과 trace를 설계 기준으로 둔다.
- 남은 검토: 에이전트별 입력 크기, 지연·메모리·비용의 수용값과 반복 측정 결과를 기록한다.

## 호환성

- 기존 근거: 여러 에이전트가 CLI, 웹 UI, 로컬 Ollama, 외부 API 등 서로 다른 실행 경계를 갖는다.
- 남은 검토: 공개 지원 OS·Python·브라우저·모델/API 조합을 정하고 빈 환경에서 설치·실행한다.

## 상호작용 능력

- 기존 근거: `README.md`에는 프로젝트별 사용 방식이 분리되어 있다.
- 남은 검토: CLI·웹 UI·메시지 채널별 오류 안내, 키보드·보조기술 사용, 언어 이해도를 실제 사용자와 점검한다.

## 신뢰성

- 기존 근거: `README.md`는 프로젝트 경계별 테스트와 외부 호출 실패 로그를 설명한다.
- 남은 검토: 네트워크 실패·시간 초과·재시도·프로세스 재시작·로그 보존에서 결과 손실과 중복 실행을 시험한다.

## 보안

- 기존 근거: `SPEC.md`는 도구 권한·외부 전송 승인 경계를 명시하고 체크리스트에 보안 검사가 연결되어 있다.
- 남은 검토: 각 에이전트의 실제 입력·도구·비밀값·프롬프트 주입 경계를 공개 버전에서 점검한다. 정적 검사 통과만으로 전체 보안을 승인하지 않는다.

## 유지보수성

- 기존 근거: 저장소와 에이전트별 명세, 프로젝트별 테스트가 있다. 공통 실행 루프는 아직 설계 단계다(`README.md`).
- 남은 검토: 외부 개발자가 프로젝트 하나를 선택해 설치·테스트·변경·문제 진단을 문서만으로 재현하는지 확인한다.

## 유연성 이식성

- 기존 근거: 개별 프로젝트가 서로 다른 모델·도구 경계를 갖는다.
- 남은 검토: 필수 외부 서비스와 로컬 대안을 분리해 표기하고, 지원 플랫폼의 깨끗한 체크아웃에서 실행한다.

## 안전성

- 기존 근거: `SPEC.md`는 승인 없는 쓰기·삭제·외부 전송을 범위에서 제외한다.
- 남은 검토: 이력서·채용공고 등 민감 입력, 이미지 생성 결과, 메시지 발송 등 프로젝트별 오용과 실패 시 안전 상태를 점검한다.

## 실사용 품질

- 기존 근거: 개발 테스트는 있으나 외부 사용자의 과업 완료·오류 해석 결과는 이 체크리스트에 없다.
- 남은 검토: 대표 사용자와 에이전트를 골라 실제 과업 완료율·시간·오류 대응을 기록한다.

## 개인정보 데이터 거버넌스

- 기존 근거: `README.md`는 사용자 입력 원문을 오류 로그에 남기지 않는다고 설명한다. 모의 면접 등 개인 정보를 받을 수 있는 프로젝트가 있다.
- 남은 검토: 프로젝트별 수집·전송·저장·삭제·접근 권한과 공개 저장소에 포함될 데이터·예제를 확인한다.

## 공급망 라이선스

- 기존 근거: 저장소 루트에서 `LICENSE` 파일을 확인하지 못했다. 외부 모델·API·라이브러리를 쓰는 프로젝트가 있다.
- 남은 검토: 원본 코드 및 생성물 권리, 의존성 목록·실제 라이선스·취약점 대응을 확인하고 공개 재사용 조건을 정한다.

## 배포 운영

- 기존 근거: `README.md`는 로컬 테스트 실행을 안내한다. 공통 하네스는 아직 설계 단계다.
- 남은 검토: 소스 공개 전 점검, 버전 고정, 릴리스 재현, 취약점 신고, 롤백·지원 담당 절차를 정한다.

## 법률 규제

- 기존 근거: 검토 지역은 전세계이고 제품별 입력·외부 전송 경계가 다르다.
- 남은 검토: 대상 지역·서비스별 개인정보, 콘텐츠 권리, 접근성 및 계약 의무를 담당자와 검토한다. 이 문서는 적합성 판단이 아니다.

## 사업 운영

- 기존 근거: 이번 범위는 소스 무료 공개 검토이며 판매·유료 지원 조건은 정해지지 않았다.
- 남은 검토: 저장소와 개별 에이전트의 대상 사용자, 문의 경로, 지원 범위와 책임자를 확정한다.
