# hermes-agents 무료 소스 공개 검토

## 2026-09-21 세션 로그와 설정 공개 범위

- 기준 커밋 `ad3ace6`의 추적 파일 93개 중 `logs/`의 과거 세션 기록 4개를 확인했다. `hermes-agents@1dc1720`에서 Git 추적을 해제하고 로컬 원본을 보존했다. `.gitignore`와 `[TRACKED-SESSION-LOG]` 회귀 검사를 추가했으며 전체 `scripts/verify_repo.ps1`이 통과했다.
- `.mcp.json`과 `.codex/config.toml`의 15개 MCP 항목, 에이전트 5개, 명령 4개는 기존 미러 검사로 일치했다. 이 검사는 설정 구조와 알려진 비밀값 패턴만 다루며, 외부 사용자의 설치 성공이나 모든 공개 적합성을 증명하지 않는다.
- 루트 `LICENSE`·`NOTICE`·`ATTRIBUTION`은 없고, 중첩 `skill-installer/LICENSE.txt`만 확인됐다. 소스 전체의 재사용 허락과 제3자 스킬 권리는 별도로 결정해야 한다. 문서의 PC별 절대 경로 5곳 및 과거 Git 커밋의 로그 노출도 공개 배포 전에 검토한다. 출시 **HOLD**, 영역 결정 `pending` 유지.

상태: **HOLD**. 기준 커밋 `1dc1720`의 에이전트 규칙·설정 예제·검증 도구 소스를 전세계에 무료 공개하는 검토다. `README.md`와 현재 체크리스트의 근거를 대조했다. 다른 독립 저장소(`ai-workspace`, `ai_prompt`)의 내용은 이 공개 범위에 포함하지 않는다. 아래 15개 영역은 모두 `pending`이다.

## 기능 적합성
- 근거: README는 설정 미러·검증 가드·새 프로젝트 생성 명령을 설명하고 체크리스트에 실행 검사가 있다. 남은 검토: 새 사용자의 설치·설정·검증 과업을 기준으로 실제 결과를 확인한다.

## 성능 효율성
- 근거: 가드와 부트스트랩 스크립트가 실행된다. 남은 검토: 지원 PC에서 초기 설정·전체 검증 시간, 자원 사용 수용값과 반복 측정을 기록한다.

## 호환성
- 근거: README는 Claude Code와 Codex CLI의 설정 차이, 집/회사 경로 차이, Windows PowerShell 명령을 설명한다. 남은 검토: 지원 버전·OS·셸·도구 조합을 빈 환경에서 확인한다.

## 상호작용 능력
- 근거: README·SETUP 문서와 오류를 내는 가드가 있다. 남은 검토: 처음 사용하는 개발자가 실패 메시지를 이해하고 설정 오류를 고칠 수 있는지 평가한다.

## 신뢰성
- 근거: 체크리스트는 가드 실행과 일부 회귀를 검증한다. 남은 검토: 훅 설치 중단·재실행·부분 파일 생성·외부 MCP 실패에서 상태 복구를 확인한다.

## 보안
- 근거: README는 비공개 자료·비밀값을 저장하지 않고 인증을 로컬에서 관리한다고 밝힌다. 체크리스트에 보안 가드가 연결되어 있다. 남은 검토: 추적된 설정·예제·명령의 실제 공개 적합성과 MCP 공급망·권한을 확인한다.

## 유지보수성
- 근거: AGENTS.md·CLAUDE.md·SETUP.md와 검증 스크립트가 있다. 남은 검토: 외부 기여자가 규칙·미러·스크립트 변경 후 필요한 검사를 문서만으로 재현한다.

## 유연성 이식성
- 근거: README는 환경별 경로 차이와 별도 저장소 관계를 설명한다. 남은 검토: 새 PC에서 필수 기능만 설치하고 다른 경로에서도 상대 참조가 동작하는지 확인한다.

## 안전성
- 근거: 훅·설정·프로젝트 생성 스크립트가 파일과 실행 권한에 영향을 준다. 남은 검토: 잘못된 경로·권한·재실행에서 기존 파일 보존과 실패 시 안전 상태를 시험한다.

## 실사용 품질
- 근거: 자동 가드는 있으나 새 사용자의 설정 성공률·오류 복구 결과는 체크리스트에 없다. 남은 검토: 다른 PC와 사용자의 설치·첫 검증 과업을 관찰한다.

## 개인정보 데이터 거버넌스
- 근거: README는 개인 설정을 저장소 밖 또는 gitignore에 두도록 안내한다. 남은 검토: 전체 추적 파일과 공개 생성물에서 개인·회사 정보와 로컬 경로를 검토하고 수정·삭제 절차를 정한다.

## 공급망 라이선스
- 근거: 저장소 루트 `LICENSE`는 확인되지 않았고 MCP 서버·외부 도구와 가져온 자료를 설명한다. 남은 검토: 코드·인용 자료·설정 예제의 권리와 도구 공급망을 조사하고 배포 조건을 정한다.

## 배포 운영
- 근거: 검증 스크립트와 훅 설치 절차가 있다. 남은 검토: 공개 릴리스·설정 호환성·문제 신고·잘못된 설정의 롤백과 유지 책임을 정한다.

## 법률 규제
- 근거: 전세계 무료 소스 공개를 검토하며 외부 자료와 설정 문서가 포함된다. 남은 검토: 저작권·개인정보·지역별 의무를 담당자와 검토한다. 이 문서는 법률 적합성 판단이 아니다.

## 사업 운영
- 근거: README는 개인 AI 작업용 저장소로 소개한다. 남은 검토: 외부 대상 사용자, 지원할 도구 버전, 문의·취약점 신고 경로와 담당자를 정한다.
