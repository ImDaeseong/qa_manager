# qa_manager 출시 검토 기록

상태: **HOLD**. 이 도구는 로컬에서 여러 저장소의 검사를 실행하고 정적 HTML 결과를 생성한다. `index.html`은 GitHub Pages로 공개된다. 아래 기록은 2026-09-20 기준 저장소와 실행 검사에서 확인한 **부분 근거 및 미완료 작업**이다. 출시 버전, 대상 고객, 배포 형태, 판매·운영 지역과 최종 승인자는 아직 확정되지 않았다. 따라서 어느 영역도 전체 통과로 판정하지 않는다.

검토 기준: [상용 소프트웨어 출시 검토 기준](RELEASE_READINESS_STANDARD.md). ISO/IEC 25010:2023은 품질 범주를 제시할 뿐 구체적인 제품 수용값을 대신 정하지 않는다. 자동 검사 통과도 ASVS 또는 WCAG 적합성 주장이 아니다.

## 기능 적합성

- 확인: `R5`는 임시 프로젝트 등록→실제 검사 실행→개별·전체 HTML 생성의 종단 흐름을 통과·실패 양쪽으로 검증한다. 기록된 `status: pass`가 실제 실패를 덮지 못한다.
- 남은 일: 대표 사용자가 실패 원인을 확인하고 수정 후 재검사하는 과업의 기대 결과와 오류 입력 수용 기준을 버전별로 정한다.

## 성능 효율성

- 확인: 검사별 시간 제한은 있으나 전체 9개 프로젝트 실행 시간·메모리·출력 크기 목표 및 측정 기록은 없다.
- 남은 일: 대상 PC와 프로젝트 수를 확정하고 전체 생성 시간·최대 메모리·HTML 크기 목표를 정한 뒤 반복 측정한다.

## 호환성

- 확인: README는 Windows 배치 실행과 Python 명령을 설명한다. 지원 Python/Windows/브라우저 버전의 매트릭스는 없다.
- 남은 일: 지원 조합과 인코딩·경로·브라우저 동작을 명시하고 각 조합에서 클린 실행한다.

## 상호작용 능력

- 확인: 정적 HTML 대시보드는 검사 상태를 표시한다. 대표 사용자 과업·키보드·스크린리더·대비 검토 기록은 없다.
- 남은 일: 사용성 과업과 접근성 목표 수준을 정하고 자동 및 사람 평가를 수행한다. 공개 웹 페이지 전체의 WCAG 적합성을 현재 주장하지 않는다.

## 신뢰성

- 확인: `R4`는 멈춘 검사 명령의 종료와 보고서 교체 실패 시 이전 완성본 보존·임시 파일 정리를 검증한다.
- 남은 일: 전체 9개 프로젝트 생성의 중간 실패·재실행·복구, 디스크 부족과 외부 저장소 부재를 검증한다. 두 회귀 검사는 전체 신뢰성을 증명하지 않는다.

## 보안

- 확인: `R1`은 경로·환경·출력·명령 경계를, `R3`은 정적 보안 패턴과 예외 주석을 검사한다.
- 남은 일: 공개 HTML에 노출될 프로젝트 이름·명령·결과의 위협 모델, 검사 명령을 등록할 권한, 종속성·배포 경로와 수동 침투 검토를 확정한다. 정적 패턴 검사로 취약점 부재를 주장하지 않는다.

## 유지보수성

- 확인: `R2`는 필수 문서 존재를 검증하고, README에 실행 명령이 있다.
- 남은 일: 새로운 프로젝트 등록·검사 실패 진단·의존성 갱신·변경 후 회귀 검증을 제3자가 문서만으로 재현하는지 확인한다.

## 유연성 이식성

- 확인: `repo_root`는 상대 경로이며 배치 진입점은 Windows를 사용한다. `R7`은 README의 상대 경로 명령으로 개별 대시보드가 생성되는지 검증한다.
- 남은 일: 지원 플랫폼의 빈 체크아웃에서 설치·설정·실행·이전 버전 결과 교체를 검증한다. 타 OS 지원은 선언하지 않는다.

## 안전성

- 확인: 검사 명령은 로컬 프로젝트 안에서 실행되므로 오용 시 파일·프로세스·공개 결과에 영향을 줄 수 있다.
- 남은 일: 잘못된 검사 명령, 민감 프로젝트 등록, 부분 생성, 승인 오해의 위해를 열거하고 실패 시 안전 상태 및 사람 확인 절차를 시험한다.

## 실사용 품질

- 확인: 개발 테스트는 있지만 실제 검수 담당자가 결함을 발견·해석·조치하는 성과 자료는 없다.
- 남은 일: 대상 역할과 사용 맥락을 정해 과업 완료율, 오류 해석, 소요 시간 및 피드백을 수집한다.

## 개인정보 데이터 거버넌스

- 확인: `R6`은 공개 HTML 생성 직전에 사용자 홈 경로와 자격증명 형태의 문자열을 거부한다. 기존 검사 출력 마스킹과 함께 작동한다. 패턴으로 인식되지 않는 민감 정보까지 막는다고 주장하지 않는다.
- 남은 일: 입력→로그→생성 HTML→GitHub Pages 데이터 흐름을 작성하고 프로젝트 이름·명령의 공개 적합성, 보관·삭제·접근·권리 절차를 결정한다. 처리 없음 판정은 아직 하지 않는다.

## 공급망 라이선스

- 확인: Python 패키지와 테스트 실행 환경을 사용한다. 배포 대상 구성요소 목록·버전 고정·라이선스 검토 기록은 없다.
- 남은 일: 런타임/빌드 의존성 SBOM, 실제 라이선스 원문, 취약점 대응, 정적 자산 재배포 권리를 확인한다.

## 배포 운영

- 확인: README는 재생성 명령과 정적 결과의 비실시간 성격을 설명한다. CI는 자체 검사를 수행하며, 생성기는 각 HTML을 임시 파일에 완성한 뒤 교체한다.
- 남은 일: 공개 전 검토, 재현 빌드, 백업·복원, 실패 알림, 롤백, 사고 대응과 지원 책임을 실행 가능한 운영 절차로 만든다.

## 법률 규제

- 확인: 판매·운영 지역, 고객 유형, 데이터 처리 역할이 정해지지 않았다.
- 남은 일: 관할별 개인정보·접근성·소비자·계약 의무를 해당 지역의 전문가와 검토한다. 이 문서는 법률 적합성 판단이 아니다.

## 사업 운영

- 확인: 가격·결제·환불·고객 지원 모델이 정의되지 않았다.
- 남은 일: 판매 여부와 고객·과금·환불·지원 정책을 책임자가 정하고 실제 운영 경로를 시험한다.

## 종료 조건

출시 범위와 각 영역의 수용 기준·실행 결과·남은 위험을 기록하고 독립적인 책임자가 검토한다. `projects/qa_manager/checklist.yaml`의 15개 `pending` 결정과 최종 승인 기록은 그때만 변경한다. 자동 검사 실패를 사람 승인으로 덮지 않는다.
