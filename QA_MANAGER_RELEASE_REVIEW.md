# qa_manager 출시 검토 기록

상태: **HOLD**. 검토 대상은 `2026-09-20-rc1`(기준 커밋 `2b10b49`와 이 검토의 변경)의 GitHub Pages 대시보드와 GitHub 소스코드를 전세계에 무료 공개하는 것이다. 소스코드에는 MIT 라이선스를 적용한다. 대상 사용자는 개발자·검수 담당자로 가정하며 책임자 확인을 기다린다. 이 도구는 로컬에서 여러 저장소의 검사를 실행하고 정적 HTML 결과를 생성한다. 아래 기록은 2026-09-20 기준 저장소와 실행 검사에서 확인한 **부분 근거 및 미완료 작업**이다. 최종 승인자는 아직 확정되지 않았다. 따라서 어느 영역도 전체 통과로 판정하지 않는다.

검토 기준: [상용 소프트웨어 출시 검토 기준](RELEASE_READINESS_STANDARD.md). ISO/IEC 25010:2023은 품질 범주를 제시할 뿐 구체적인 제품 수용값을 대신 정하지 않는다. 자동 검사 통과도 ASVS 또는 WCAG 적합성 주장이 아니다.

## 출시 과업과 공개 경계

- 사용 사례: 개발자·검수 담당자가 여러 로컬 저장소의 등록된 검사 명령을 실행해 요구사항별 상태를 보고, 실패 시 로컬 CLI에서 원인을 확인한다. 대상 역할은 책임자 확인 전의 가정이다.
- 공개 데이터 흐름: `checklist.yaml`의 프로젝트명·설명·검사 명령 → 로컬 명령 실행 → 통과/실패 상태 → 정적 `index.html`과 개별 대시보드 → GitHub Pages. 명령 stdout은 HTML에 넣지 않는다. 생성된 10개 HTML의 정적 구조 검사에서 외부 자산·스크립트·깨진 내부 링크는 0건이었고, 각 페이지는 `lang=ko`, `h1` 하나, `details`/`summary` 수 일치였다. 이는 브라우저·보조기술 사용성 판정이 아니다.
- 방문자 데이터: [GitHub Pages 공식 문서](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)는 방문자 IP를 보안 목적으로 기록한다고 명시한다. 서비스의 데이터 처리 역할과 고지 의무는 확인 전이다.

## 사용자 평가와 게시 절차

- 실제 대상 사용자에게 (1) 검사 통과와 출시 보류를 구별하기, (2) 한 프로젝트의 검사항목과 실행 명령 찾기, (3) 실패 시 `run_checklist.py`의 로컬 출력으로 원인 찾기를 수행하게 한다. 각 과업의 성공 여부·소요 시간·오해·도움 요청을 기록하고, 세 과업 모두 도움 없이 완료하며 출시 보류를 배포 승인으로 오해하지 않아야 수용한다. 아직 참여자 결과는 없다.
- 게시 전 `python -m unittest discover -s tests -q`, `python scripts/generate_system_index.py`, `python scripts/check_public_paths.py`와 CI의 나머지 가드를 실행한다. 생성된 HTML·체크리스트·README·라이선스 변경 내용을 사람이 읽어 공개 가능성을 확인하고, Pages 게시 소스 설정과 책임자 승인을 기록한다. 현재 Pages 설정은 인증된 관리 화면에서 확인하지 못했다.
- 게시 후 공개 URL의 최신 버전·링크·상태를 확인한다. `e27c030`의 [검증 CI](https://github.com/ImDaeseong/qa_manager/actions/runs/35507492427)와 [Pages 배포](https://github.com/ImDaeseong/qa_manager/actions/runs/35507491905)는 성공했다. 공개 `index.html`은 HTTP 200이며 로컬 생성물과 줄바꿈 정규화 후 내용이 일치했고, 실제 브라우저에서 9개 프로젝트 모두 검사 통과·출시 보류로 표시됐다. 오류가 보이면 배포 책임자가 마지막 정상 커밋으로 복구하고 원인·영향·복구 시간을 기록한다. 실제 롤백 연습과 지원 담당자 지정은 남아 있다.

## 기능 적합성

- 확인: `R5`는 임시 프로젝트 등록→실제 검사 실행→개별·전체 HTML 생성의 종단 흐름을 통과·실패 양쪽으로 검증한다. 기록된 `status: pass`가 실제 실패를 덮지 못한다.
- 남은 일: 대표 사용자가 실패 원인을 확인하고 수정 후 재검사하는 과업의 기대 결과와 오류 입력 수용 기준을 버전별로 정한다.

## 성능 효율성

- 확인: 검사별 300초 시간 제한이 있다. 이번 Windows 환경에서 9개 프로젝트 전체 재생성은 393.89초였고 실패는 0건이었다. 최종 생성 파일은 `index.html` 145,976바이트, 가장 큰 개별 대시보드 25,251바이트였다.
- 남은 일: 지원 PC·프로젝트 수와 허용 시간·메모리·HTML 크기 목표를 책임자가 정하고 반복 측정한다. 현재 값은 한 환경의 1회 기준선이며 최대 메모리는 측정하지 않았다.

## 호환성

- 확인: README는 Windows 배치 실행과 Python 명령을 설명한다. 이번 로컬 검증 환경은 Windows NT 10.0.19045와 Python 3.14.5·PyYAML 6.0.2다. Python 3.13/Linux 검증 CI도 `e27c030`에서 통과했다. 공개 페이지는 Chrome에서 열어 상태 문구와 프로젝트 링크를 확인했다. 지원 브라우저·보조기술 매트릭스는 없다.
- 남은 일: 지원 조합과 인코딩·경로·브라우저 동작을 명시하고 각 조합에서 클린 실행한다.

## 상호작용 능력

- 확인: 정적 HTML 대시보드는 검사 상태를 표시한다. 생성 HTML의 언어·제목·`details`/`summary` 연결과 내부 링크를 정적으로 확인했다. `R8`은 두 테마의 일반 텍스트 색상 토큰을 4.5:1 이상으로 검사한다. 밝은 테마 보류 배지의 4.24:1 결함을 4.86:1로 수정하고 재발 검사를 연결했다. 로컬 파일 브라우저 열기는 보안 정책으로 차단됐지만 공개 GitHub Pages는 Chrome에서 열어 요약 표시를 확인했다.
- 남은 일: [WCAG 2.2 AA](https://www.w3.org/TR/WCAG22/#conformance)를 목표로 키보드·스크린리더·실제 렌더링 대비와 사용자 과업을 사람과 브라우저에서 평가한다. 토큰 검사만으로 공개 웹 페이지 전체의 WCAG 적합성을 주장하지 않는다.

## 신뢰성

- 확인: `R4`는 멈춘 검사 명령의 종료와 보고서 교체 실패 시 이전 완성본 보존·임시 파일 정리를 검증한다. 첫 공개 커밋의 Linux CI에서 멈춘 자식 프로세스가 출력 파이프를 잡고 있어 15초 수용값 대신 약 30초가 걸리는 결함을 확인했다. POSIX 실행을 별도 프로세스 그룹으로 시작하고 타임아웃 때 그룹 전체를 종료하도록 수정했다. 이번 전체 재생성에서 `ai_test2`의 CareerDiff 단위 검사가 한 번 실패했고, 같은 체크리스트를 즉시 다시 실행하자 11개 항목 모두 통과했다. 최초 실패의 원인은 확인되지 않았다.
- 남은 일: CareerDiff 단위 검사의 일시적 실패 원인을 재현·기록하고, 전체 9개 프로젝트 생성의 중간 실패·재실행·복구, 디스크 부족과 외부 저장소 부재를 검증한다. 두 회귀 검사는 전체 신뢰성을 증명하지 않는다.

## 보안

- 확인: `R1`은 경로·환경·출력·명령 경계를, `R3`은 정적 보안 패턴과 예외 주석을 검사한다. 공개 보고서 생성기는 등록된 9개 프로젝트 이름만 허용하고 새 이름은 `PUBLIC_PROJECT_UNREVIEWED`로 차단한다. 전체 생성 시작 전에 검사하므로 미검토 프로젝트 때문에 기존 HTML 일부만 바뀌지 않는다.
- 남은 일: 허용된 프로젝트 안에서 바뀌는 설명·명령·결과의 공개 적합성, 검사 명령을 등록할 권한, 종속성·배포 경로와 수동 침투 검토를 확정한다. 이름 허용 목록과 정적 패턴 검사로 민감정보·취약점 부재를 주장하지 않는다.

## 유지보수성

- 확인: `R2`는 필수 문서 존재를 검증하고, README에 실행 명령이 있다.
- 남은 일: 새로운 프로젝트 등록·검사 실패 진단·의존성 갱신·변경 후 회귀 검증을 제3자가 문서만으로 재현하는지 확인한다.

## 유연성 이식성

- 확인: `repo_root`는 상대 경로이며 배치 진입점은 Windows를 사용한다. `R7`은 README의 상대 경로 명령으로 개별 대시보드가 생성되는지 검증한다. 기본 9개 체크리스트는 이 작업공간의 형제 저장소를 요구하므로 소스만 새로 복제한 환경에서 전체 재검사는 실행되지 않는다. 공개 HTML 열람은 별도다.
- 남은 일: 지원 플랫폼의 빈 체크아웃에서 설치·설정·실행·이전 버전 결과 교체를 검증한다. 타 OS 지원은 선언하지 않는다.

## 안전성

- 확인: 검사 명령은 로컬 프로젝트 안에서 실행되므로 오용 시 파일·프로세스·공개 결과에 영향을 줄 수 있다.
- 남은 일: 잘못된 검사 명령, 민감 프로젝트 등록, 부분 생성, 승인 오해의 위해를 열거하고 실패 시 안전 상태 및 사람 확인 절차를 시험한다.

## 실사용 품질

- 확인: 개발 테스트는 있지만 실제 검수 담당자가 결함을 발견·해석·조치하는 성과 자료는 없다.
- 남은 일: 대상 역할과 사용 맥락을 정해 과업 완료율, 오류 해석, 소요 시간 및 피드백을 수집한다.

## 개인정보 데이터 거버넌스

- 확인: 체크리스트의 프로젝트명·설명·검사 명령과 실행 후 통과/실패 상태가 HTML로 이동한다. `render_test_item()`은 명령의 stdout 내용을 HTML에 넣지 않는다. `R6`은 공개 HTML 생성 직전에 사용자 홈 경로와 자격증명 형태의 문자열을 거부하며, `check_public_paths.py`는 공개 문서·체크리스트·HTML에서 로컬 계정 경로를 검사한다. 기존 문서에 있던 사용자 홈 경로 8곳을 상대 경로로 바꿨다. 새 프로젝트의 HTML 자동 공개는 이름 허용 목록으로 차단한다. 패턴으로 인식되지 않는 민감 정보와 기존 프로젝트 내용 변경까지 막는다고 주장하지 않는다.
- 남은 일: 기존 프로젝트 이름·설명·명령의 변경분을 게시 전에 사람이 검토하고, 공개 소스 저장소에 새 체크리스트를 올리기 전에도 검토한다. GitHub Pages 방문자 데이터의 보관·삭제·접근·권리 절차와 역할을 결정한다. 처리 없음 판정은 하지 않는다.

## 공급망 라이선스

- 확인: 프로젝트 소스의 MIT 라이선스는 `LICENSE`에 명시했다. 직접 외부 구성요소는 PyYAML 6.0.2와 CI의 `actions/checkout@v5`·`actions/setup-python@v6`이다. 각 버전의 실제 [PyYAML LICENSE](https://raw.githubusercontent.com/yaml/pyyaml/6.0.2/LICENSE), [checkout LICENSE](https://raw.githubusercontent.com/actions/checkout/v5/LICENSE), [setup-python LICENSE](https://raw.githubusercontent.com/actions/setup-python/v6/LICENSE)에서 MIT 허가·고지 조항을 확인했다. CI 설치 대상을 `requirements.txt`의 PyYAML 6.0.2로 고정했다.
- 남은 일: 런타임·빌드·GitHub Actions 구성요소의 전체 목록과 실제 라이선스 원문, 취약점 대응, 설치 아티팩트의 무결성 및 정적 자산 재배포 권리를 확인한다. 로컬 PyYAML 확인만으로 전체 공급망 검토가 끝나지 않는다.

## 배포 운영

- 확인: README는 재생성 명령과 정적 결과의 비실시간 성격을 설명한다. CI는 자체 검사를 수행하며, 생성기는 각 HTML을 임시 파일에 완성한 뒤 교체한다. `e27c030`의 CI와 Pages 배포 성공, 공개 첫 페이지 HTTP 200 및 로컬 생성물 내용 일치를 확인했다.
- 남은 일: 공개 전 검토, 재현 빌드, 백업·복원, 실패 알림, 롤백, 사고 대응과 지원 책임을 실행 가능한 운영 절차로 만든다.

## 법률 규제

- 확인: 검토 대상 운영 지역은 전세계이며 무료 공개다. 대상 사용자는 개발자·검수 담당자로 임시 가정했고 데이터 처리 역할은 정해지지 않았다.
- 남은 일: 관할별 개인정보·접근성·소비자·계약 의무를 해당 지역의 전문가와 검토한다. [EU 집행위원회 안내](https://commission.europa.eu/law/law-topic/data-protection/reform/rules-business-and-organisations/application-regulation/who-does-data-protection-law-apply_en)는 EU 대상 무료 서비스에도 GDPR이 적용될 수 있다고 설명한다. 이번 전세계 배포가 해당 요건에 드는지, GitHub Pages IP 기록에 대한 역할과 고지를 어떻게 정할지는 법률 검토가 필요하다. 이 문서는 법률 적합성 판단이 아니다.

## 사업 운영

- 확인: 이번 검토 범위는 전세계 공개 무료 배포다. 가격·결제·환불 흐름은 계획하지 않았다. 사용자 지원 방식은 정의되지 않았다.
- 남은 일: 대상 사용자와 지원·문의 경로를 책임자가 확정하고 실제 운영 경로를 시험한다.

## 종료 조건

출시 범위와 각 영역의 수용 기준·실행 결과·남은 위험을 기록하고 독립적인 책임자가 검토한다. `projects/qa_manager/checklist.yaml`의 15개 `pending` 결정과 최종 승인 기록은 그때만 변경한다. 자동 검사 실패를 사람 승인으로 덮지 않는다.
